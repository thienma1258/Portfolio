

function Layout(props) {
    const {
        // mobileTopBar,
        // mobilePageHeader,
        // footer,
        children,
      } = props;

  return (
    <div>
        {children}
    </div>
  )
}

export default Layout